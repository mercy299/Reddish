version: '3'
services:
  reddishserver:
    image: 'reddish-server'
    build: ./server
    restart: always
    ports:
      - 3005:3005
    environment:
      MONGODB_URI: 
      OPENAI_API_KEY: mongodb+srv://reddish:reddish@cluster0.4mlajb4.mongodb.net/?retryWrites=true&w=majority
      CLOUDINARY_CLOUD_NAME: 
      CLOUDINARY_API_KEY: 
      CLOUDINARY_API_SECRET:

  reddishclient:
    image: 'reddish-client'
    build: ./client
    restart: always
    ports:
      - 80:80
    depends_on:
      - reddishserver